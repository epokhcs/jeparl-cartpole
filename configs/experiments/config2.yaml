# Configuration 2: JEPA + RL gradients (Best performing)

defaults:
  - ../base_config

experiment:
  name: "config2_jepa_with_rl_gradients"

configuration:
  type: 2
  use_jepa: true
  use_rl_gradients: true
  use_regularization: false
