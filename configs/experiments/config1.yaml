# Configuration 1: Baseline (No JEPA, only RL gradients)

defaults:
  - ../base_config

experiment:
  name: "config1_baseline_no_jepa"

configuration:
  type: 1
  use_jepa: false
  use_rl_gradients: true
  use_regularization: false
